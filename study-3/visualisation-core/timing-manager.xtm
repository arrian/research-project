;; Timing Manager
;;
;; This manager handles all musical timing and source code timing.

(bind-func get_time_previous
  (lambda (widget)
    0))

(bind-func get_time_next
  (lambda (widget)
    0))

(bind-func get_percent_complete
  (lambda (widget)
    1.0))









